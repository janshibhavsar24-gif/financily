// Application entry point.
//
// Responsibilities:
//   - Import the global Tailwind stylesheet (index.css) so styles
//     are included in the bundle.
//   - Mount the React application into the #root div in index.html.
//   - Use React 18's createRoot API (not the legacy ReactDOM.render).
//   - StrictMode is enabled to surface double-render bugs during development.
//     It has no effect on the production build.

// TODO: import './index.css'
// TODO: import React from 'react'
// TODO: import ReactDOM from 'react-dom/client'
// TODO: import App from './App'

// TODO: ReactDOM.createRoot(document.getElementById('root')!).render(
//   <React.StrictMode>
//     <App />
//   </React.StrictMode>
// )
